<!-- @loom-file release=0.12.08 revision=2 policy=package-priority -->
# LOOM reusable module: header-bar

Provides the project header region. `widthMode=fit-content` now performs true shrink-to-content layout with normal header padding while preserving left/center/right placement.
