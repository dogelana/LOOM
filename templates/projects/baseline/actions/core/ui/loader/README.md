<!-- @loom-file release=0.12.08 revision=2 policy=package-priority -->
# LOOM reusable module: loader

Bootstrap loader for LOOM projects. v0.11.16 uses a short minimum dwell and relies on the runtime's non-blocking telemetry queue so visual progress reflects module work rather than telemetry round trips.
