<!-- @loom-file release=0.12.08 revision=3 policy=package-priority -->
# LOOM reusable module: logo

The logo module includes alpha-masked pointer-reactive shine; transparent pixels never receive the reflection.

## Responsive host stability (0.12.05)

Desktop/mobile logo-slot dimensions are CSS-responsive instead of being chosen once in JavaScript, keeping the logo stable across responsive boundaries.
