<!-- @loom-file release=0.12.08 revision=2 policy=package-priority -->
# Project Update Log

Optional project-scoped release history. Install in a project only when that project maintains its own `CHANGELOG.md`. The LOOM platform update log belongs on LOOM Home.
