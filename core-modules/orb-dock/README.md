# LOOM Core · Orb Dock

`core.ui.orb-dock` is release-managed and project-scoped. It captures project content modules into the footer quick-access area. If zero modules are configured for capture, Orb Dock suppresses the complete More Tools row instead of rendering an empty label/container.
