# Project Toast Theme

`core.ui.toast-theme` does not create a second toast system. It is a project-scoped provider for the shared LOOM feedback service. By default it inherits the project's canonical main/accent colors. Admin may switch the project to LOOM defaults or custom colors.
